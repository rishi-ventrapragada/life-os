# SESSION_5.md — Step 5: Supabase Auth — email/password login + per-user RLS (the login gate goes up; anon becomes real)

**Goal:** put a real login in front of the app. Email/password auth via Supabase Auth (only), convert the owner's existing anonymous user into a permanent account **without changing its user id** (so the already-seeded `life_areas` + goals carry over with no migration), move the idempotent 5-area seed to fire on first *real* login, prove RLS isolates two real accounts adversarially, then clean up the stray anonymous users and revisit the two advisor WARNs that were parked for this step.
**Done when (from PRD §8):** logged-out users see the login screen only; two real test accounts cannot see (or write) each other's goals. Verified by *actually logging in as both accounts and running cross-account reads/writes*, then committed, pushed, and confirmed live on Vercel.

## Kickoff prompt (paste as first message)

> New session. Read CLAUDE.md, PRD.md, SECURITY.md, and SESSION_5.md fully. We are on **Step 5 of the PRD build plan: Supabase Auth (email/password login) with per-user RLS.**
> Enter plan mode and propose the plan for:
> - **Email/password login via Supabase Auth exclusively** (SECURITY.md) — never hand-rolled hashing, storage, comparison, or reset. Logged-out users see the login screen only (PRD §8 DoD); all app content gates behind an authenticated session. Keep session handling on Supabase defaults (no custom token storage or expiry tinkering); respect Supabase Auth's built-in rate limits — no bypass endpoints.
> - **Anon→real conversion preserving the user id:** convert the owner's current anonymous user into a permanent email/password account via `updateUser` (attach email + password), so the owner's already-seeded `life_areas` and goals stay put — no data migration. Do the owner's conversion *before* purging any anonymous users.
> - **Seed-on-first-login:** rework `ensureSessionAndAreas()` in `lib/bootstrap.ts` so the idempotent 5-area seed fires on first real login/sign-up — it must stop auto-calling `signInAnonymously()` on page load. Keep the promise-singleton + `unique(user_id, name)` idempotency.
> - **Adversarial RLS test with two real accounts** (SECURITY.md): account A attempts to read *and write* account B's `life_areas`/`goals` rows and vice versa; every attempt must fail. Observed with real selects/inserts, not assumed.
> - **Anon-user cleanup:** after the gate is live and the owner is converted, disable anonymous sign-ins and purge the stray anonymous users (their rows cascade-delete via the FK) from the Supabase dashboard.
> - **Advisor-WARN revisit:** the two accepted "Anonymous Access Policies" WARNs on `life_areas`/`goals` come due now. With anonymous sign-ins disabled (and/or an `is_anonymous = false` exclusion added to the policies), re-run `get_advisors(security)` and confirm the resulting state — don't assume it cleared.
> - This step needs a schema/policy migration only if you add the `is_anonymous` exclusion → in that case flip `read_only=true` off in `.mcp.json` first (temporary), reconnect MCP, apply the migration, then re-lock (SECURITY.md Step 3 item). If the WARN clears purely by disabling anonymous sign-ins in the dashboard, no migration is needed and the lock stays on — decide which in the plan.
> Work in small increments; verify by actually signing in as two accounts and watching cross-account reads/writes get rejected. End of session: commit, push, and I'll confirm the login gate + goal isolation live on Vercel.

## Before starting (2 min)
- [ ] Open VS Code at `C:\dev\Personal Life OS` (NOT the old OneDrive path if it appears in Open Recent)
- [ ] Claude Code: Step 5 **is on the fable-mode list** — invoke the skill with the plan (this is the sequencing-sensitive, security-critical step)
- [ ] Have the Supabase dashboard open (Auth → Users, Auth → Providers/Email settings, table editor) and the Vercel URL handy: https://life-os-lac-tau.vercel.app/
- [ ] Know before you start: adding an `is_anonymous` policy exclusion flips MCP `read_only` off temporarily, then re-locks — the user edits `.mcp.json` and reconnects MCP

## Session rules (unchanged)
- One step only. New ideas → FUTURE.md.
- Approve read-only commands quickly; read write/install commands fully.
- Two failed fixes = wrong diagnosis → invoke fable-mode.
- Never end the session without commit + push.

## Notes carried over from Step 4 (+ the Step 3 security thread this step closes)
- **Sequencing is the trap this step — order deliberately (this is why Step 5 is on the fable-mode list):** (1) build login + the logged-out gate; (2) convert the *owner's existing anonymous user* to a permanent email/password account, preserving its user id, so the data you've been testing with survives; (3) create the *second* real account for adversarial testing; (4) run cross-account read/write attempts and confirm every one fails; (5) disable anonymous sign-ins + purge the stray anon users; (6) revisit the advisor WARNs. **Purging anon users before converting the owner would delete the owner's seeded data** (FK `on delete cascade`).
- **Anon→real conversion preserves the user id — no migration** (design settled since Step 3; the `lib/bootstrap.ts` header comment states it). Attach email/password to the anonymous user via `updateUser` rather than minting a fresh account; the seeded `life_areas`/goals stay put because `user_id` never changes.
- **`ensureSessionAndAreas()` (`lib/bootstrap.ts`) changes shape this step.** Today it auto-`signInAnonymously()`s on page load and then seeds via idempotent upsert (`onConflict: "user_id,name"`, `ignoreDuplicates`). Step 5 must stop the auto-anon-on-load (logged-out users see login only) and move the seed to fire after a real login/sign-up. Keep the promise-singleton (guards React StrictMode double-mount) and the `unique(user_id, name)` idempotency.
- **MCP is `read_only=true` right now** (`.mcp.json:5`, re-locked at the end of Step 3). You only need to flip it off if you add an `is_anonymous` policy exclusion (a migration). If so: user removes `&read_only=true`, reconnects MCP, apply migration, then the user re-appends it and reconnects (SECURITY.md Step 3 item). If the WARN is cleared purely by disabling anonymous sign-ins in the dashboard, no migration runs and the lock never comes off.
- **The two "Anonymous Access Policies" advisor WARNs on `life_areas`/`goals` are due now.** They flagged the anonymous-sign-in design itself (anon users hold the `authenticated` role); owner-only `(select auth.uid()) = user_id` still isolated everyone, which is why they were accepted at Step 3. Resolution for the plan: v1 is owner-only with login required (PRD §2), so disabling anonymous sign-ins entirely is the clean path — and/or add an `is_anonymous = false` exclusion to the policies. Re-run `get_advisors(security)` and confirm the state after; don't assume it cleared.
- **Stray anonymous users to purge:** Step 3's verification runs left 3+ anon users, each carrying 5 cascaded `life_areas` rows; every page load since (auto-anon-on-load) has minted more. Purge them from the Supabase dashboard *after* the owner's conversion. Keep not sharing the URL until the gate lands.
- **SECURITY.md Step 5 items are Definition-of-Done gates, observed not assumed:** Supabase Auth only (no hand-rolled hashing/storage/reset); adversarial two-account RLS on every RLS table; auth pages rate-limit aware (respect Supabase's built-in limits, no bypass endpoints); session handling on Supabase defaults (no custom token storage/expiry). Plus the always-on items: secret-scan the staged diff before commit, service-role key never in client code, `.env*` stays git-ignored, dependencies npm-only.
- **Only `life_areas` + `goals` carry RLS so far** — those are the two tables to test adversarially. Tasks/habits/courses/etc. don't exist yet (Steps 6+); each re-runs this checklist when it's born.
- **Data layer files:** `lib/bootstrap.ts` + `components/goals/useGoals.ts` (there is no `lib/goals.ts`). Auth likely adds a session context/provider + a login form component; whatever content the app shows for a logged-in user gates behind the authenticated session. Keep every component under ~200 lines (Law 1) and propose a split when approaching.
- **`getTodayIST()` now exists** (`lib/dates.ts`, Step 4 done; vitest edge tests in `lib/dates.test.ts`, run via `npm test`). Step 5 doesn't need it, but it stays the only clock. `vitest` is the test runner added in Step 4. `npm audit`'s 2 moderate pre-existing findings (Next's bundled postcss) stay parked as the pre-v2 audit item.
- **Vercel env vars are already set** (`NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY`, all environments, since Step 3). The anon/publishable `sb_publishable_…` key is client-safe; the service-role key bypasses RLS and never ships to the client (no `NEXT_PUBLIC_` prefix, never in a client path).
- Windows gotchas: kill node via PowerShell (`Get-Process node | Stop-Process -Force`), not bash `pkill`; stop the dev server before `npm run build` (shared `.next`); if styles look stale, diff the *served* CSS against disk before re-editing.
- Claude self-verifies with headless Chrome + `playwright-core` in its scratchpad (worked through Steps 2–3) — but this step's core evidence is behavioral: actually signing in as two real accounts and watching cross-account reads/writes get rejected, plus the `get_advisors(security)` output.

**After DoD passes: stop. Step 6 (Tasks/To-do section — repeat the CRUD + persistence pattern solo, now behind real auth) is the next session's quest.**
