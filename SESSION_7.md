# SESSION_7.md — Step 7: Habits section — CRUD + daily completion checkbox + persistence behind real auth (two tables this time; streaks/grid are NOT this step)

**Goal:** build the Habits section as a full vertical slice — two new tables (`habits` and `habit_checks`, each born with `user_id` + owner-only RLS in the same DDL) plus their UI and Supabase wiring — reusing the data-layer + design patterns proven in Goals (Step 3) and Tasks (Step 6), gated behind the real auth from Step 5. Per PRD §8 Step 7 and §9: a habit has name + life area + add/edit/archive; each habit shows **today's completion checkbox**; checking today writes exactly one `habit_checks` row, unchecking removes it, and a `unique(habit_id, check_date)` constraint makes a day impossible to double-log. `check_date` is a plain `yyyy-mm-dd` IST date from `getTodayIST()` (Law 3).
**Done when (from PRD §8):** checking today's box **creates one `habit_checks` row**; unchecking **removes it**; the day is **unique per habit** (no double-log, enforced by the DB constraint, not just the UI); habit CRUD (add/edit/archive) + today's checked state **persist across a hard refresh**; and **both new RLS tables survive the adversarial two-account cross-read/write test**. Verified by *actually using the deployed app* (add habit → check today → refresh → still checked; uncheck → refresh → still unchecked; re-check spam doesn't create duplicates) and the adversarial RLS run on `habits` **and** `habit_checks`, then committed, pushed, and confirmed live on Vercel.

## Kickoff prompt (paste as first message)

> New session. Read CLAUDE.md, PRD.md, SECURITY.md, and SESSION_7.md fully. We are on **Step 7 of the PRD build plan: the Habits section — CRUD + daily completion checkbox + `habit_checks`, behind real auth.** This step is **on the fable-mode list** — apply the `fable-mode` skill.
> Enter plan mode and propose the plan for:
> - **Two tables via one migration, each born with `user_id` + owner-only RLS in the same DDL** (Architecture Law 4 + SECURITY.md Step 3 — RLS is part of the CREATE, never a follow-up):
>   - `habits`: `id uuid pk default gen_random_uuid()`, `user_id uuid default auth.uid()` FK→`auth.users`, `area_id uuid` FK→`life_areas`, `name text not null`, `is_archived boolean not null default false`, `created_at timestamptz default now()`.
>   - `habit_checks`: `id uuid pk default gen_random_uuid()`, `user_id uuid default auth.uid()` FK→`auth.users`, `habit_id uuid not null` FK→`habits` (**consider `on delete cascade`** so deleting a habit clears its checks — but mind [[tasks-fk-no-cascade]]: if not cascade, the app must delete checks before the habit), `check_date date not null` (**plain `yyyy-mm-dd` IST from `getTodayIST()`**, Law 3), `created_at timestamptz default now()`, and a **`unique(habit_id, check_date)`** constraint so a day can't be double-logged at the DB level.
>   - For **each** table, the four per-command policies `{table}_{select,insert,update,delete}_own` on role `authenticated` with `(select auth.uid()) = user_id` (USING for select/update/delete, WITH CHECK for insert/update) — mirror the `goals`/`tasks` policy shape exactly.
>   - Applying this **requires the MCP lock dance** (MCP is `read_only=true` now): I remove `&read_only=true` from `.mcp.json` + reconnect MCP → you `apply_migration` (one migration, both tables + all eight policies + the unique constraint) → I re-append it + reconnect (SECURITY.md Step 3: MCP returns to read-only after schema work).
> - **The daily checkbox as insert/delete, not a boolean column.** Checking today = insert one `habit_checks` row with `check_date = getTodayIST()`; unchecking = delete that row. "Checked today?" is derived by whether a row exists for `(habit_id, getTodayIST())`. Never a mutable `done` flag on `habits`. The `unique(habit_id, check_date)` constraint is the real guard against double-logging — the UI's disabled/optimistic state is a convenience, not the source of truth; a duplicate insert must be handled gracefully (treat unique-violation as "already checked", not an error).
> - **Re-run the adversarial RLS checklist against BOTH new tables** — every new RLS table re-runs it when born (this is now the 4th and 5th such tables after `life_areas`, `goals`, `tasks`). Use the **two-throwaway-accounts pattern from `scripts/rls-test.mjs`** (git-ignored; anon key + real JWTs; A creates a habit + a check; B attempts cross-account read *and* write on `habits` and on `habit_checks`; every attempt must fail; confirm ground truth via MCP, which bypasses RLS; delete both test accounts after — **children before parents**, [[tasks-fk-no-cascade]] + [[mcp-readonly-blocks-cleanup-writes]]: cleanup DELETEs run in the dashboard SQL editor, not read-only MCP). Space out signups — free-tier email is rate-limited to a few/hour ([[supabase-free-tier-auth-constraints]]).
> - **All date logic through `getTodayIST()`** (Law 3): `check_date` is stored as the raw `yyyy-mm-dd` string from `getTodayIST()`; "checked today?" compares against `getTodayIST()`. No raw `new Date()` date math, no UTC, no custom clocks/counters.
> - **Reuse the established data-layer + design patterns:** a `useHabits()` hook shaped like `components/tasks/useTasks.ts` (awaits `ensureAreasSeeded(userId)` from `lib/bootstrap.ts` for `areaIdByName`, optimistic writes with resync-on-error, `useSession()` for the user id, fetches habits + today's checks together) and `components/habits/{types,HabitCard,HabitForm}.tsx` mirroring the `tasks/` folder; fill/replace the `components/sections/HabitsSection.tsx` stub. Habit cards use the **`GlowCard` soft variant** (grid-safe) with the today-checkbox as the primary interactive element (hover + focus-visible + active; animate only transform/opacity). Keep every component under ~200 lines (Law 1). Archived habits are hidden from the active list (an `is_archived=false` filter), not deleted.
> - **Advisors check after the migration:** re-run `get_advisors(security)` once both tables exist + RLS is on; confirm no new WARN (expect only the accepted Pro-gated leaked-password WARN). Don't assume; read the output.
> - **Carried from AUDIT_1 — three findings this step must fix in-flight, not inherit or copy** (see AUDIT_1.md §2/§3 for the full evidence):
>   - **D1 — the null-`area_id` add-before-ready race (must fix in Habits; must NOT replicate).** In the Goals/Tasks pattern the "+ Add" control renders while `status === "loading"` — before `ensureAreasSeeded` populates `areaIds.current`. A fast submit in that window sends `area_id = undefined`, the row lands with `area_id = null`, and the `life_areas!inner(name)` join then hides it from every future fetch **permanently** (an invisible orphan while the UI shows "save failed"). Step 7's Habits UI must **gate/disable the "+ Add habit" control (and the today-checkbox) until the seed/session is ready** (`status === "ready"`), and **`useHabits` must NOT copy the unguarded pattern** — guard in the hook too (bail or surface an error if `areaIds.current` is empty rather than inserting a null-area row). Cheap follow-up: note a backfill-fix for the same race in `GoalsSection`/`TasksSection` in FUTURE.md (don't fix Goals/Tasks this step — one step only — just record it).
>   - **S1 — the adversarial RLS re-test must be the FULL matrix, not Step 6's narrower one.** Step 6's recorded run was 4 checks in one direction (B reads/updates/deletes A's row + A-intact re-read) and never exercised the cross-owner **INSERT (WITH CHECK)** path; the "16/16" figure was Step 5's, not Step 6's. For Step 7, **extend `scripts/rls-test.mjs`** so the test on **both** `habits` and `habit_checks` runs **both directions** (A-attacks-B *and* B-attacks-A) **and** exercises the **cross-owner INSERT** — each user attempts to insert a row carrying the *other* user's `user_id` (and an FK to the other's habit for `habit_checks`), which the `WITH CHECK ((select auth.uid()) = user_id)` policy must reject with `new row violates row-level security policy`. **The Step 7 RLS gate's Definition of Done is the complete matrix passing** — read/update/delete return 0 rows *and* forced-`user_id` inserts are blocked, both directions, both tables.
>   - **S2 — schema hygiene in the migration window (MCP is unlocked anyway).** Give the **new** tables the invariants Law 4 rides on from birth: `habits.user_id` and `habit_checks.user_id` **`NOT NULL`** (not just defaulted), plus the **FK/user_id indexes** the performance advisor wants (`user_id` and `area_id` on `habits`; `user_id` and `habit_id` on `habit_checks` — the `unique(habit_id, check_date)` constraint already covers `habit_id`-leading lookups, so judge whether a separate `habit_id` index is redundant). **Also fold in the parked `tasks` fixes** — `ALTER TABLE tasks ALTER COLUMN user_id SET NOT NULL` (AUDIT_1 confirmed **0 null rows**, so it's safe) + the two missing indexes `tasks_user_id_idx` and `tasks_area_id_idx` — **but only as a clean, separately-reviewed migration** (its own `apply_migration` call, reviewed on its own), since the write window is already open. **If folding the `tasks` fix in complicates or risks the Habits migration in any way, keep it as its own separate migration/step and say so explicitly** — do not entangle the two. The Habits tables are the priority; the `tasks` tidy-up is opportunistic.
> Work in small increments (schema+RLS for both tables → adversarial FULL-matrix re-test on both → habit CRUD UI+wiring → today-checkbox insert/delete + duplicate-safety → verify live). End of session: commit, push, and I'll confirm habit CRUD + today-check persistence + no-double-log live on Vercel.

## Before starting (2 min)
- [ ] Open VS Code at `C:\dev\Personal Life OS`.
- [ ] **My real owner account** (`rishiventrapragada23@gmail.com`) exists, is confirmed, and auto-seeded its 5 `life_areas` — verified at the end of Step 6. **Claude: re-confirm via MCP** that it's still the only real user and its `life_areas` are intact before building on it.
- [ ] Have the Supabase dashboard open (table editor, Auth → Users, SQL editor) and the Vercel URL handy (https://life-os-lac-tau.vercel.app/).
- [ ] Step 7 **is on the fable-mode list** — apply `fable-mode`. The migration (two tables + unique constraint + eight policies) and the adversarial RLS re-test on both tables are the security-critical parts; slow down there.
- [ ] Know the MCP lock dance is coming (migration needs write access): I edit `.mcp.json` to drop `&read_only=true`, reconnect; Claude migrates; I re-append it, reconnect.

## Session rules (unchanged)
- One step only. New ideas → FUTURE.md.
- Approve read-only commands quickly; read write/install commands fully.
- Two failed fixes = wrong diagnosis → invoke fable-mode.
- Never end the session without commit + push.

## Explicitly NOT this step (guard against scope creep)
- **Streaks (current + max) and the GitHub-style monthly completion grid are Step 8, NOT Step 7.** PRD §8 splits them: Step 7 is CRUD + today's checkbox + `habit_checks` rows; Step 8 computes streaks/max-streak from those rows and renders the month grid. Do **not** build streak math or the grid this step — just create the correct `habit_checks` rows that Step 8 will read. If the itch to add them appears, it goes to FUTURE.md or waits for Step 8.
- **The Today section** (which will surface "habits due today") is Step 9. Build only the Habits section here.

## Notes carried over (standing)
- **AUDIT_1 findings folded into this step** — see the kickoff's "Carried from AUDIT_1" block for the actionable detail: **D1** (gate the "+ Add habit" control + today-checkbox on `status === "ready"`, and don't clone the unguarded null-`area_id` race into `useHabits`), **S1** (RLS re-test is the FULL both-directions + cross-owner-INSERT matrix, not Step 6's narrower check), **S2** (`NOT NULL` `user_id` + FK indexes on the new tables from birth; fold in the `tasks.user_id` NOT NULL + two missing indexes only as a clean separate migration). Full evidence in `AUDIT_1.md` §2/§3.
- **MCP is `read_only=true` right now** (`.mcp.json`). The Step 7 migration is the only write this step needs → do the lock dance, then re-lock immediately after (SECURITY.md Step 3). All RLS *testing* uses the anon key + real JWTs via the throwaway-accounts script; **cleanup DELETEs run in the dashboard SQL editor** (read-only MCP can't DELETE, anon key can't touch `auth.users`) — [[mcp-readonly-blocks-cleanup-writes]].
- **Adversarial RLS is a per-table gate.** Proven so far: `life_areas`, `goals`, `tasks` (all passed). `habits` + `habit_checks` are the **4th and 5th** — they re-run the same two-account cross-read/write checklist the moment they're born. Reuse/extend `scripts/rls-test.mjs`.
- **FK cleanup order** — FKs to `auth.users` are not ON DELETE CASCADE ([[tasks-fk-no-cascade]]); throwaway-account cleanup deletes children before parents (`habit_checks` → `habits` → `tasks`/`goals`/`life_areas` → `auth.users`), atomically in the dashboard SQL editor. Decide `habit_checks.habit_id` FK cascade at migration time — if it's `on delete cascade`, deleting a habit auto-clears its checks (simpler app-side delete); if not, `useHabits().deleteHabit` must delete checks first.
- **`tasks` is the working template** for the whole slice: table shape + the four per-command `(select auth.uid()) = user_id` policies on `authenticated`; the `components/tasks/` folder (`types.ts`, `TaskCard.tsx`, `TaskForm.tsx`, `useTasks.ts`) + `TasksSection.tsx`; optimistic writes with resync-on-error; `GlowCard` soft variant for grid cards; the accent-button / field-input class strings. Copy the shape, swap the fields; add the checkbox insert/delete flow.
- **Date law (Law 3):** `getTodayIST()` in `lib/dates.ts` (vitest edge tests in `lib/dates.test.ts`, `npm test`). `check_date` is the raw `yyyy-mm-dd` from `getTodayIST()`; "checked today?" compares against it. No raw `Date`, no UTC.
- **Auth is in place** (Step 5): `components/auth/SessionProvider.tsx` (`useSession()` → `{ session, status }`; rejects stale anon sessions), `AuthGate.tsx`, `LoginForm.tsx`. `useHabits()` gets the user id from `useSession()` and only runs inside the gate, exactly like `useTasks()`.
- **Parked, accepted for v1 (do not re-chase this step):**
  - **Leaked Password Protection** — Pro-plan-gated on this free-tier project, so its security-advisor WARN can't be cleared. Accepted for v1; revisit only if the project upgrades to Pro. ([[supabase-free-tier-auth-constraints]])
  - **`npm audit`** — two moderate findings (Next's bundled postcss) remain parked as the pre-v2 audit item.
  - **Custom SMTP before going multi-user** — default Supabase auth email is rate-limited to a few/hour, which slowed the RLS re-tests; if this app ever opens beyond the single owner, wire a real SMTP provider first. Parked idea → belongs in FUTURE.md, not this step.
- **Env/deploy unchanged:** `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` set in Vercel (all envs); anon/publishable key is client-safe; service-role key never ships to the client. Push to `main` auto-deploys.
- **Windows gotchas** ([[windows-dev-loop-gotchas]]): kill node via PowerShell (`Get-Process node | Stop-Process -Force`), not bash `pkill`; stop the dev server before `npm run build` (shared `.next`); self-verify UI with headless installed Chrome + scratchpad `playwright-core`. The **localhost React hydration warning is the Grammarly extension**, not app code ([[grammarly-hydration-warning-not-ours]]) — don't "fix" it. The **behavioral** DoD evidence (check today → hard refresh → still checked; uncheck → persists; re-check doesn't duplicate; adversarial RLS rejections) is what counts, not "should work."

## Locked decisions (2026-07-20, mid-Step-7) — RESUME HERE

Increment 1 shipped migration `20260720122754 create_habits_and_habit_checks` (both tables, 8 policies, unique constraint, indexes) and `20260720122759 harden_tasks_user_id_and_indexes` (`tasks.user_id` NOT NULL + the two missing indexes). Reviewing that schema surfaced two locked decisions.

### Decision A — `habit_checks` INSERT must verify HABIT OWNERSHIP, not just `user_id`

**The shipped policy is the weak form.** Read back from `pg_policies` this session:

```
habit_checks_insert_own | INSERT | with_check: (( SELECT auth.uid() AS uid) = user_id)
```

No `EXISTS` on `habits`. **The hole:** an attacker who knows a victim's `habit_id` inserts `(victim_habit_id, today)` stamped with their **own** `user_id` — which satisfies `user_id = auth.uid()`. Combined with `unique (habit_id, check_date)`, that row occupies the victim's slot for that habit/day, so the owner's own check insert fails with 23505. The victim can't SELECT or DELETE the row (their policies scope to their own `user_id`) — an invisible, unfixable-from-the-app **denial of service on their own habit**.

**The fix:** the INSERT `WITH CHECK` must confirm **both** `user_id = auth.uid()` **and** that the referenced habit belongs to `auth.uid()`, via `EXISTS (SELECT 1 FROM habits h WHERE h.id = habit_checks.habit_id AND h.user_id = auth.uid())`. Keep `unique (habit_id, check_date)` as-is.

*Honest scoping:* exploiting it needs a victim's `habit_id` UUID, which RLS never hands out — with one real user the practical risk today is ~zero. It matters because v2 opens registration (PRD §2); the policy must be correct regardless of the current user count.

**Generalized into law:** SECURITY.md "Step 3 — database" now carries the rule — *child-table INSERT policies must verify parent-row ownership via `EXISTS` on the parent, not merely `user_id = auth.uid()`.* Applies to every future child table (`assignments`→`courses`, `workout_logs`, etc.).

### Decision B — archive is a column: `archived_at timestamptz NULL`

`archived_at` replaces the `is_archived boolean` that Increment 1 shipped (and that PRD §6 specified). **NULL = active; a timestamp = archived**, preserving *when*. Every "active habits" query filters `archived_at IS NULL`. Archiving removes a habit from today's checklist but **must NOT delete its `habit_checks`** — Step 8 streaks need that history. `habits` had 0 rows when this was decided, so the column swap carried no data risk. PRD §6 line corrected to match.

### Pending migration — the RLS matrix must prove the FINAL policy

`harden_habit_checks_insert_and_archive_column` (Decision A policy replacement + Decision B column swap) is **pending** and must land in a proper MCP unlock window **before** the adversarial RLS matrix runs. The matrix must never certify a stale policy.

**The S1 matrix gains a new required case:** an attacker inserting a `habit_checks` row that references a habit they do **not** own, stamped with their **own** honest `user_id`, must be **REJECTED**. Under the old policy this would have *succeeded* — this case is exactly what proves Decision A landed.

### MCP lock-dance note (learned the hard way this session)

Two `/mcp` reconnects failed to rebind the session to the read-only URL — the file on disk was correct while the live session stayed write-capable. Only a **full VS Code window reload** (extension-host restart) re-read `.mcp.json`. Also: **never test the lock with a write probe.** Two probes ran before this was settled; the second left a junk migration row `20260720123536 readonly_lock_verification_expect_rejection` in `supabase_migrations.schema_migrations` (owner removes it in the dashboard — migration-bookkeeping writes don't go through Claude). Read-only status isn't observable from the tool list (`apply_migration` is advertised either way; `read_only` is enforced server-side at call time), so after a reload the lock is **user-asserted**, not verified.

**After DoD passes: stop. Step 8 (Habit streaks + monthly grid — computed from the `habit_checks` rows this step creates) is the next session's quest.**
