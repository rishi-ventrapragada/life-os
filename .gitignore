# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — NEVER commit secrets
.env
.env.*
!.env.example

# throwaway RLS adversarial test scripts (contain no secrets, but never shipped)
/scripts/rls-test.mjs

# vercel
.vercel

# claude code — per-machine approvals, not shared
.claude/settings.local.json

# typescript
*.tsbuildinfo
next-env.d.ts
